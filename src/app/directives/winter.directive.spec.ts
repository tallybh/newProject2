import { WinterDirective } from './winter.directive';

describe('WinterDirective', () => {
  it('should create an instance', () => {
    const directive = new WinterDirective();
    expect(directive).toBeTruthy();
  });
});
